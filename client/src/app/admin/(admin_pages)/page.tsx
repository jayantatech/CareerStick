import React from "react";

const Admin = () => {
  return (
    <div className="flex items-center justify-center min-h-screen min-w-screen">
      <h1 className="text-4xl font-semibold">Welcome to Admin</h1>
    </div>
  );
};

export default Admin;
